{% extends 'base.html.twig' %}

{% block body %}
    {% include ('application/slidenav.html.twig') %}
    <div class="shadow-lg rounded container form-container mt-5 ">
        <h1 class="text-center">Edit</h1>
        <hr>
        <div class="mt-5 col-8 col-lg-6 mx-auto">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="d-flex flex-column d-md-flex flex-md-row justify-content-between p-3" >
                <input type="submit" class="shadow-lg  btn btn-primary mb-1 mr-1" value="modifier" />
                {{ form_end(form) }}
                <a href="{{ path('cheptel_species', {'tag' : tag}) }}" class="btn btn-secondary mb-1">Retour liste</a>

                {{ form_start(delete_form) }}
                <input class="btn btn-danger" type="submit" value="Supprimer">
                {{ form_end(delete_form) }}
            </div>
        </div>
    </div>

{% endblock %}


